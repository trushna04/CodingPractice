#https://leetcode.com/problems/remove-element/

class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        l=(len(nums)-1)
        i=0;
        while(i<=l):
            if(nums[i]==val):
                nums.pop(i)
                l=l-1
            else:
                i=i+1
        return (len(nums))
